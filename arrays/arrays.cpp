#include<iostream>
using namespace std;
int main(){
    // how to take input in the arrays . 1st declare size of the arrays and initalise the arrays and start for loop
    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    // SUM OF THE ARRAYS IS GIVEN BY
    int sum=0;
    for(int i=0;i<n;i++){
        sum =sum+a[i];
    }
}

// how to take input in the arrays
int main(){
    int n;
    cin>>n;
    a[n];
    for(int i=0;i<n;i++){
        cin>>a[i];
        
    }
}
